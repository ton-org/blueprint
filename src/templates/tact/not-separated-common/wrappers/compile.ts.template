{{name}}.compile.ts
import { CompilerConfig } from '@ton-ai-core/blueprint';

export const compile: CompilerConfig = {
    lang: 'tact',
    target: '{{contractPath}}',
    options: {
        debug: true,
    },
};
